name: Repository checks

on:
  pull_request:
  push:
    branches: [main]

jobs:
  source-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check tracked source files
        run: ./scripts/check_project.sh
